apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: todo-app-11
  template:
    metadata:
      labels:
        app: todo-app-11
    spec:
      containers:
      - name: todo-app-11
        image: trainingapcacr.azurecr.io/todo-app:trainingxx-x
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: todo-app-service-11
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: todo-app-11

